#!/bin/bash

set -e
set -x

export PATH=$PATH:/usr/local/bin

iid=$(curl --silent http://169.254.169.254/latest/meta-data/instance-id)

export AWS_ACCESS_KEY={{ ec2_key }}
export AWS_SECRET_KEY={{ ec2_secret }}

ec2-terminate-instances $iid
